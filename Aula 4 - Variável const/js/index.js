/* const (Constante) é uma variável cujo valor é fixo
que não pode ser mudado ou reatribuída. OBS: Não pode começar sendo nula. */

const nome = "Felipe Camargo de Almeida";

document.write("<h1>Nome: " + nome + "</h1>"); 
document.write("<h3>" + typeof nome + "</h3>");